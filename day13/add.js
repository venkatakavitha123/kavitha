const add = (x,y)=>x+y;
const sum = (a,b,callback)=>
{
  const calculating=()=>console.log("calculating")  
  const sum =(a,b,add)=>x=a+b;
  calculating();
}
console.log(sum(4,5));
//console.log(sum())   undefined